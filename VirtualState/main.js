const createStore = (reducer, initialState) => {
  const getState = () => {};

  const dispatch = () => {};

  const subscribe = () => {};

  return {
    getState,
    dispatch,
    subscribe
  };
};

const reducer = () => {};

const store = createStore(reducer, 'initial state');
// store.getState
// store.dispatch
// store.subscribe
